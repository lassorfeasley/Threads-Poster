fastapi>=0.111
uvicorn>=0.30
# Efficient file watcher for uvicorn --reload (avoids polling the whole tree,
# including data/ and .venv, which burns CPU and freezes the UI on every edit).
watchfiles>=0.21
jinja2>=3.1
python-multipart>=0.0.9
sqlalchemy>=2.0
pyyaml>=6.0
python-dotenv>=1.0
requests>=2.32
# YouTube is running an experiment that makes PO tokens mandatory on the
# android_vr client, which 403s every download. The `visionos` client is the
# escape hatch and only exists in builds after the 2026.07.04 stable, so a
# nightly is required until the next stable ships: pip install -U --pre yt-dlp
yt-dlp>=2026.08.18
youtube-transcript-api>=0.6.2
# Local speech-to-text for operator-uploaded clips (CPU, no API key)
faster-whisper>=1.0
# Renders the stylized caption overlays (app/subtitles.py)
pillow>=10.0
anthropic>=0.30
supabase>=2.5

# Needed when DATABASE_URL points at Supabase/Postgres
psycopg2-binary>=2.9